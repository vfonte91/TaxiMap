<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taximap"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="17" />

    <permission
        android:name="com.example.taximap.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.taximap.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/maps_api_key" />

        <uses-library android:name="com.google.android.maps" />

        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name" >
        </activity>
 	
        <activity
            android:name=".Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.example.taximap.Login" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Account"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".map.TestSwitchMap"
            android:label="@string/generic_map_label" >
        </activity>
        <activity
            android:name="com.example.taximap.test.CustomerMap"
            android:label="@string/generic_map_label" >
        </activity>
        <activity
            android:name=".map.DriverMap"
            android:label="@string/generic_map_label" >
        </activity>
        <activity
            android:name=".map.MainActivity"
            android:label="@string/generic_map_label" >
        </activity>
        <activity
            android:name=".map.TabLayoutActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".map.MapViewActivity" />
        <activity android:name=".map.MapActivity" />
        <activity android:name=".map.ListActivity" />
        <activity android:name=".map.FilterActivity" >
        </activity>
        <activity android:name=".map.HelpActivity" />
    </application>

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />
    <!-- OpenGL ES V2 support -->

</manifest>